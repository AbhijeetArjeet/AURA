services:
  - type: web
    name: ypdlp
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app --bind 0.0.0.0:$PORT
    rootDir: website
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
    plan: free
